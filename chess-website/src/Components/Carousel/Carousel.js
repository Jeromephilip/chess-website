import React from 'react';

function Carousel() {

    // This is purely for the images on the side. We need to make a smooth transition with
    // arrow keys. We will use this component in the landing page
    return (
        <div>
            
        </div>
    );
}

export default Carousel;